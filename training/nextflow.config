includeConfig 'salmon-arm.config'

docker {
  enabled = true
}

report.overwrite = true
timeline.overwrite = true

aws {
  client {
    anonymous = true
  }
}

params {
    max_cpus      = 10
    max_memory    = '12GB'
    max_time      = '2.h'
}
