# CLAUDE.md

Project-specific rules live in `.claude/rules/` and load automatically every
session via the `@`-imports below. Global planning rules (docs/plans structure,
commit messages, markdown formatting, etc.) are covered by the `docs` plugin,
deployed globally — no need to duplicate them here.

@.claude/rules/00-dgx-spark-docker.md
